package com.example.todolistai.features.task_main.ui

import androidx.compose.runtime.Composable
import com.example.todolistai.core.data.local.model.Task

@Composable
fun TaskItem(task: Task, onTaskCheckedChange: (Boolean) -> Unit, onDeleteTask: () -> Unit) {
    // Implement the UI for a single task item
}
