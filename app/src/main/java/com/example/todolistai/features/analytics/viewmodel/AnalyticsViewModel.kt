package com.example.todolistai.features.analytics.viewmodel

import androidx.lifecycle.ViewModel
import com.example.todolistai.core.data.repository.TaskRepository

class AnalyticsViewModel(private val taskRepository: TaskRepository) : ViewModel() {
    // Implement the logic for providing analytics data to the UI
}
